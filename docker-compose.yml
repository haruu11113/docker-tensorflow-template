version: "3"
services:
  dev:
    build:
      context: .
      dockerfile: ./infra/docker/tensorflow/Dockerfile
    ports:
    - "8888:8888"
    - "5000:5000"
    volumes:
        - ./repo:/work/repo
