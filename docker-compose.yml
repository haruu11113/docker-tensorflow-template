version: "3"
services:
  tf:
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    build:
      context: .
      dockerfile: ./infra/docker/tensorflow/Dockerfile
    ports:
    - "8888:8888"
    volumes:
        - ./repo:/home/ec2-user/repo/
